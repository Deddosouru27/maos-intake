# Server
PORT=3001

# AI / LLM
ANTHROPIC_API_KEY=
OPENAI_API_KEY=           # embeddings (text-embedding-3-small) — optional, dedup degrades without it
GROQ_API_KEY=             # Whisper transcription for Instagram/audio
COHERE_API_KEY=           # reranking — optional

# Supabase — Pitstop (ideas, ingested_content, context_snapshots, entity_nodes)
PITSTOP_SUPABASE_URL=https://stqhnkhcfndmhgvfyojv.supabase.co
PITSTOP_SUPABASE_ANON_KEY=

# Supabase — maos-memory (deprecated, leave empty)
MEMORY_SUPABASE_URL=
MEMORY_SUPABASE_ANON_KEY=

# Scraping
APIFY_API_TOKEN=          # Instagram scraper via Apify
APIFY_TOKEN=              # GitHub Trending via Apify junipr/github-trending-scraper

# GitHub Parsing Pipeline (Cluster 6)
GITHUB_TOKEN=             # GitHub API — README fetch, commit diff (read:public_repo scope)
GOOGLE_CREDENTIALS_JSON=  # GCP service account JSON string (for BigQuery on Vercel)
                          # Locally: set GOOGLE_APPLICATION_CREDENTIALS=/path/to/sa.json instead
CRON_SECRET=              # Shared secret for Railway→Vercel cron calls (X-Cron-Secret header)

# Telegram alerts (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Proxy (optional, for YouTube behind geo-block)
PROXY_WORKER_URL=

# Misc
RELEVANCE_THRESHOLD=0.2
